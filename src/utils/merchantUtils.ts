import { MerchantDetails } from "../../src/models/MerchantDetails";

export async function getMerchantDetails(
  merchantId: string
): Promise<MerchantDetails> {
  // Add a return statement here
  return {} as MerchantDetails;
}
